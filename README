# TODO

- Verify feature functionality of clusters
  - View cluster
    - TLS termination
    - Sign in
    - Supply chains
    - Security dashboard
    - APIs
    - App accelerators
  - Build cluster
  - Run cluster
  - Iterate cluster
- Add certificates
- Add DNS management
- Add SSO

# Build a docker image to use with 'act'.  Note that the platform setting is only needed on Apple chips.
docker build -t act:aaron-latest --platform linux/x86_64 .

# Use 'act' to test github actions locally.
## First, pull an image to run docker on docker.
docker pull docker
docker run -it --rm --privileged -v /var/run/docker.sock:/var/run/docker.sock -v /Users/uaaron/git/tap-on-aks:/home --platform linux/x86_64 docker

## Second, install curl and act.
apk update
apk add curl
curl https://raw.githubusercontent.com/nektos/act/master/install.sh | sh
cd /home

## Run your action using the image you built earlier.
act workflow_dispatch --secret-file /home/.local/secrets.env -P ubuntu-latest=act:aaron-latest -j AddTls



Why does the tap-gui.tap2.ullom.xyz cert not work when stored in the default namespace?
- Delete certificate and secret
- Update tap-gui httpproxy to remove tls setting
- Deploy view cluster configuration again
- !!! Something rolls through every once-in-a-while and removes the cert from the httpproxy
